# dongler
Dongler is a Rust-native document extraction engine with Python and JavaScript bindings that turns messy files into structured data. Extract text, tables, layout, and metadata from PDFs and future formats like Excel and HTML into Markdown, LaTeX, JSON, and a unified document IR through pluggable extraction engines.
