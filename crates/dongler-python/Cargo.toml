[package]
name = "dongler-python"
version = "0.3.1"
edition = "2021"
license = "MIT"
authors = ["Daniel Fat"]
description = "Created by Daniel Fat. Python bindings for the Dongler Rust document extraction core."
repository = "https://github.com/cristianexer/dongler"
publish = false

[lib]
name = "_dongler"
crate-type = ["cdylib"]

[dependencies]
dongler-core = { path = "../dongler-core", version = "0.3.1" }
pyo3 = { version = "0.23", features = ["extension-module", "abi3-py39"] }
serde_json = "1.0"
