[package]
name = "dongler"
version = "0.3.1"
edition = "2021"
license = "MIT"
authors = ["Daniel Fat"]
description = "Created by Daniel Fat. Command-line interface for Dongler document extraction."
readme = "../../README.md"
repository = "https://github.com/cristianexer/dongler"

[dependencies]
dongler-core = { path = "../dongler-core", version = "0.3.1" }
clap = { version = "4.5", features = ["derive"] }
